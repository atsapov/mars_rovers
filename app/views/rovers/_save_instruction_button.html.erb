<% flash.each do |key, value| %>
  <%= content_tag(:div, value, :class => "flash #{key}") %>
<% end %>
<%= form_for(:rover, :url => instruction_path) do |f| %>
  <div class="field">
    <label> Enter instruction </label><br>
    <%= f.text_field :instruction %>
  </div>
  <div class="actions">
    <%= f.submit "Go" %>
  </div>
<% end %>

